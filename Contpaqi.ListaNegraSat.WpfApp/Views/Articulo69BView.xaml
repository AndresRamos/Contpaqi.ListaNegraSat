<UserControl x:Class="Contpaqi.ListaNegraSat.WpfApp.Views.Articulo69BView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:viewModels="clr-namespace:Contpaqi.ListaNegraSat.WpfApp.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Contpaqi.ListaNegraSat.WpfApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:Articulo69BViewModel}"
             dialog:DialogParticipation.Register="{Binding}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Expander Grid.Row="0"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Header="Artículo 69-B, primer párrafo del CFF"
                  IsExpanded="True"
                  BorderThickness="2"
                  Margin="5">
            <TextBlock Text="Cuando la autoridad fiscal detecte que un contribuyente ha estado emitiendo comprobantes sin contar con los activos, personal, infraestructura o capacidad material, directa o indirectamente, para prestar los servicios o producir, comercializar o entregar los bienes que amparan tales comprobantes, o bien, que dichos contribuyentes se encuentren no localizados, se presumirá la inexistencia de las operaciones amparadas en tales comprobantes."
                       TextWrapping="Wrap"
                       Margin="5" />
        </Expander>
        <Grid Grid.Row="1"
              Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <GroupBox Header="Contribuyentes Incumplidos SAT"
                      BorderThickness="2"
                      Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1"
                            Content="Cargar"
                            Command="{Binding CargarCommand}"
                            Width="100"
                            HorizontalAlignment="Left"
                            Margin="5"
                            Style="{DynamicResource SquareButtonStyle}" />
                    <Label Grid.Row="1"
                           Grid.Column="0"
                           Content="Filtro:"
                           FontWeight="Bold"
                           Margin="5" />
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding Filtro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             controls:TextBoxHelper.Watermark="Filtro"
                             controls:TextBoxHelper.ClearTextButton="True"
                             Margin="5" />
                </Grid>
            </GroupBox>
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal">
                <Label Content="Total:"
                       FontWeight="Bold"
                       Margin="5" />
                <TextBlock Text="{Binding ContribuyentesCount}"
                           VerticalAlignment="Center"
                           Margin="5" />
                <Label Content="Total Filtro:"
                       FontWeight="Bold"
                       Margin="5" />
                <TextBlock Text="{Binding ContribuyentesCollectionView.Count}"
                           VerticalAlignment="Center"
                           Margin="5" />
            </StackPanel>
            <ListView Grid.Row="2"
                      ItemsSource="{Binding ContribuyentesCollectionView}"
                      SelectedItem="{Binding ContribuyenteSatSeleccionado}"
                      Style="{StaticResource VirtualisedMetroListView}"
                      SelectionMode="Single"
                      Margin="5">
                <ListView.Resources>
                    <ContextMenu x:Key="ItemContextMenu">
                        <MenuItem Header="Copiar RFC"
                                  Command="{Binding Path=DataContext.CopiarRfcContribuyenteSatCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}" />
                    </ContextMenu>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}"
                           BasedOn="{StaticResource MetroListViewItem}">
                        <Setter Property="ContextMenu"
                                Value="{StaticResource ItemContextMenu}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="RFC"
                                        DisplayMemberBinding="{Binding Rfc}" />
                        <GridViewColumn Header="Razon Social"
                                        DisplayMemberBinding="{Binding RazonSocial}" />
                        <GridViewColumn Header="Supuesto"
                                        DisplayMemberBinding="{Binding Supuesto}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <Grid Grid.Row="1"
              Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <GroupBox Header="Contribuyentes Incumplidos Contpaq"
                      BorderThickness="2"
                      Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1"
                            Content="Cargar"
                            Command="{Binding CargarContpaqCommand}"
                            Width="100"
                            HorizontalAlignment="Left"
                            Margin="5"
                            Style="{DynamicResource SquareButtonStyle}" />
                    <Label Grid.Row="1"
                           Grid.Column="0"
                           Content="Filtro:"
                           FontWeight="Bold"
                           Margin="5" />
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding FiltroContpaq, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             controls:TextBoxHelper.Watermark="Filtro"
                             controls:TextBoxHelper.ClearTextButton="True"
                             Margin="5" />
                </Grid>
            </GroupBox>
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal">
                <Label Content="Total:"
                       FontWeight="Bold"
                       Margin="5" />
                <TextBlock Text="{Binding ContribuyentesContpaqCount}"
                           VerticalAlignment="Center"
                           Margin="5" />
                <Label Content="Total Filtro:"
                       FontWeight="Bold"
                       Margin="5" />
                <TextBlock Text="{Binding ContribuyentesContpaqCollectionView.Count}"
                           VerticalAlignment="Center"
                           Margin="5" />
            </StackPanel>
            <ListView Grid.Row="2"
                      ItemsSource="{Binding ContribuyentesContpaqCollectionView}"
                      SelectedItem="{Binding ContribuyenteContpaqSeleccionado}"
                      Style="{StaticResource VirtualisedMetroListView}"
                      SelectionMode="Single"
                      Margin="5">
                <ListView.Resources>
                    <ContextMenu x:Key="ItemContextMenu">
                        <MenuItem Header="Copiar RFC"
                                  Command="{Binding Path=DataContext.CopiarRfcContribuyenteContpaqCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}" />
                    </ContextMenu>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}"
                           BasedOn="{StaticResource MetroListViewItem}">
                        <Setter Property="ContextMenu"
                                Value="{StaticResource ItemContextMenu}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="RFC"
                                        DisplayMemberBinding="{Binding Rfc}" />
                        <GridViewColumn Header="Codigo"
                                        DisplayMemberBinding="{Binding Codigo}" />
                        <GridViewColumn Header="Razon Social"
                                        DisplayMemberBinding="{Binding RazonSocial}" />
                        <GridViewColumn Header="Supuesto"
                                        DisplayMemberBinding="{Binding Supuesto}" />
                        <GridViewColumn Header="Tipo"
                                        DisplayMemberBinding="{Binding Tipo}" />
                        <GridViewColumn Header="Esta Activo"
                                        DisplayMemberBinding="{Binding EstaActivo}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal">
                <Button Content="Exportar"
                        Command="{Binding ExportarContribuyentesContpaqCommand}"
                        Margin="5"
                        Width="100"
                        Style="{StaticResource AccentedSquareButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
